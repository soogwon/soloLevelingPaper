# Gap Analysis: paper-concept-path-mcp

> Date: 2026-08-29 | Iteration: 2 | Design: `docs/02-design/features/paper-concept-path-mcp.design.md`

---

## Match Rate: 90.0%

완전 일치 1점, 부분 일치 0.5점, 미구현 0점으로 계산했다.

```text
(완전 일치 34 + 부분 일치 4 × 0.5) / 전체 40 × 100 = 90.0%
```

1차 분석의 80.0%에서 구조화 로깅, 명시적 MCP 출력 스키마, 응답 제한 경고, OpenAlex `select`, 5xx `Retry-After`, 사용량 헤더 통계와 관련 테스트를 보완했다. 핵심 로컬 MCP 완료 기준과 Report 진입 기준인 90%를 충족했다.

## 검증 기준

- 설계의 MCP 계약, 제공자 정책, 알고리즘, 보안, 테스트 및 배포 기준을 40개 체크포인트로 분리했다.
- 코드 근거는 `src/`, 자동 검증 근거는 `tests/`, 운영 근거는 `README.md`, `.env.example`, `package.json`에서 확인했다.
- 2026-08-29 재검증에서 타입 검사, 7개 테스트 파일의 35개 테스트, 빌드 및 `git diff --check`가 통과했다.

## 영역별 결과

| 영역 | 전체 | 일치 | 부분 | 미구현 | 환산 점수 |
|------|-----:|-----:|-----:|-------:|----------:|
| 아키텍처·기술 선택 | 5 | 5 | 0 | 0 | 5.0 |
| MCP 계약 | 7 | 7 | 0 | 0 | 7.0 |
| OpenAlex 제공자 | 10 | 9 | 1 | 0 | 9.5 |
| 경로 알고리즘·도메인 | 9 | 7 | 2 | 0 | 8.0 |
| 문서·보안·운영 | 4 | 4 | 0 | 0 | 4.0 |
| 테스트·품질 평가 | 5 | 2 | 1 | 2 | 2.5 |
| **합계** | **40** | **34** | **4** | **2** | **36.0 / 40** |

## Implemented Items

### 아키텍처·기술 선택 — 5/5

- [x] TypeScript, Node.js 20 이상, ESM과 고정 버전 lockfile을 사용한다.
- [x] `createServer()`와 stdio 진입점을 분리했다.
- [x] 서비스, 도메인, 제공자 및 기반 기능의 책임을 분리했다.
- [x] 프로세스 내 TTL/LRU 캐시를 사용하고 데이터베이스를 도입하지 않았다.
- [x] Vitest와 MCP 인메모리 전송으로 결정론적 검증 기반을 구성했다.

### MCP 계약 — 7/7

- [x] `search_papers`, `resolve_paper`, `trace_concept_path` 세 도구를 등록했다.
- [x] 문자열 길이, 숫자 범위, 연도 순서 및 기본값을 Zod로 검증한다.
- [x] 공개 JSON을 `snake_case`로 반환한다.
- [x] 성공 결과에 `content`와 `structuredContent`를 함께 반환한다.
- [x] 각 도구가 실제 필드 구조를 가진 명시적 Zod 출력 스키마를 공개한다.
- [x] 20,000자 text 제한 시 유효한 요약 JSON과 구조화된 `CONTENT_TRUNCATED` 경고·생략 문자 수를 반환한다.
- [x] 오류를 내부 스택이나 비밀값 없이 구조화된 공개 오류로 변환한다.

### OpenAlex 제공자 — 9/10

- [x] 응답을 런타임 스키마로 검증하고 내부 모델로 정규화한다.
- [x] API 키를 `Authorization: Bearer` 헤더로만 전송한다.
- [x] 기본 HTTPS 검증과 테스트용 HTTP 허용 설정을 제공한다.
- [x] 요청별 타임아웃과 상위 취소 신호를 결합한다.
- [x] 429와 일시적 5xx에서 `Retry-After`를 적용하고 최대 2회 재시도한다.
- [x] 성공 응답 TTL/LRU 캐시와 404의 5분 음성 캐시를 제공한다.
- [x] 요청 20회, 크레딧 및 예상 비용 상한을 도구 호출 단위로 적용한다.
- [x] OpenAlex 요청에 필요한 Work 필드만 `select`한다.
- [x] 실제 사용 크레딧, 잔여량 및 추정치와 실제값 차이를 사용량·구조화 로그에 기록한다.

### 경로 알고리즘·도메인 — 7/9

- [x] backward, forward, both 방향과 깊이·후보·노드·시간 제한을 적용한다.
- [x] 직접 인용, Topic, Keyword, 목표 주제 및 시간 방향 가중치를 적용한다.
- [x] 목표 주제가 없을 때 Topic과 Keyword로 가중치를 재분배한다.
- [x] 실제 인용과 추론 관계를 `inferred`로 구분한다.
- [x] 간선 Evidence를 최대 5개로 제한하고 템플릿 설명을 제공한다.
- [x] 순환 없는 경로, 간선 0.25, 경로 0.35 및 최대 길이 제한을 적용한다.
- [x] 결정론적 선택, 3개 이상 노드 우선, `score_margin` 및 부분 결과 경고를 제공한다.

### 문서·보안·운영 — 4/4

- [x] README에 설치, 실행, 설정, 출처, 비용, 개인정보 및 학술적 주의사항을 설명했다.
- [x] `.env.example`을 제공하고 실제 비밀값·PDCA 런타임 상태를 Git에서 제외했다.
- [x] 임의 사용자 URL 대신 설정된 OpenAlex 호스트만 호출한다.
- [x] `LOG_LEVEL`을 적용한 JSON Lines stderr 로거가 요청 ID, 도구명, 시간, 캐시·요청·비용 및 상태를 기록하고 검색어·비밀값은 기록하지 않는다.

### 테스트·품질 평가 — 2/5

- [x] 점수, 경로, 캐시, 제목 모호성, 식별자 및 로거 단위 테스트를 제공한다.
- [x] 모의 fetch와 MCP 인메모리 전송으로 실제 네트워크 없는 제공자·계약 테스트를 제공한다.

## Partial Items

| # | 설계 항목 | 현재 구현 | 남은 차이 |
|---|-----------|-----------|-----------|
| P-1 | 동시 OpenAlex 요청 기본 3개 제한 | 탐색이 순차 실행되어 동시성은 1 | 안전 상한은 충족하지만 설계한 병렬 성능을 사용하지 않는다. |
| P-2 | 반복 주제 페널티와 상세 동률 기준 | 기하평균, 길이 페널티, 직접 인용 수, ID 기준 적용 | 반복 주제, 목표 관련도 및 연도 동률 기준이 단순화돼 있다. |
| P-3 | DOI 중복·버전 처리 | `version_group_key` 계산과 ID 중복 제거 | 동일 DOI 그룹화와 버전 관계 경고는 없다. |
| P-4 | 설계된 통합 테스트 전체 범위 | 35개 테스트로 주요 회귀·계약·캐시·deadline 기반 재시도·취소·예산·로깅 검증 | 전체 8초 부분 결과, 비용 상한, 3개 이상 실제 경로의 전용 테스트가 남았다. |

## Missing Items

| # | 설계 항목 | 상태 | 우선순위 |
|---|-----------|------|----------|
| M-1 | 최소 5개 분야 품질 평가 데이터셋 | 평가 입력·기대 논문·평가자 기록 없음 | 중간 — 릴리스 품질 근거 |
| M-2 | Inspector/선택적 라이브 인수 검증 기록 | README 명령만 있고 반복 가능한 결과 기록 없음 | 낮음 — 로컬 인수 근거 |

## Changed Items (Deviations from Design)

- [x] 설계의 `src/tools/*`는 현재 규모에 맞춰 `src/server.ts`에 통합했다.
- [x] `domain/relationship.ts` 책임은 `domain/scoring.ts`의 `relationshipOf()`에 통합했다.
- [x] 제공자 인터페이스에 호출별 사용량 조회·초기화 메서드를 추가했다.
- [x] `explored`와 구조화 로그에 크레딧·예상 비용 정보를 추가했다.
- [x] 충분한 3개 노드 경로가 없으면 2개 노드 부분 경로와 `SHORT_PARTIAL_PATH` 경고를 반환한다.

## Iteration 2 Changes

- [x] `src/infrastructure/logger.ts` 구조화 stderr 로거 추가
- [x] `LOG_LEVEL` 및 stdio 시작·오류 로그 연결
- [x] 세 도구의 명시적 MCP 출력 스키마 추가
- [x] text 생략 시 structuredContent 경고와 생략 문자 수 추가
- [x] OpenAlex Work 요청의 `select` 필드 제한 추가
- [x] 5xx `Retry-After` 반영
- [x] 크레딧 실제값·잔여량·추정 차이 기록
- [x] 로거, 출력 스키마, text 제한, 5xx 재시도 및 사용량 헤더 테스트 추가
- [x] 헤더 부재 시 예상 크레딧 유지와 기본 지수 백오프 회귀 수정
- [x] 숫자 초·HTTP 날짜형 `Retry-After` 파싱 및 테스트 추가
- [x] 내장·외부 로거 실패가 MCP 도구 결과에 영향을 주지 않도록 격리
- [x] 임의의 5초 재시도 대기 상한 제거
- [x] 검색·확인 10초, 경로 탐색 8초 전체 deadline 적용
- [x] `Retry-After` 대기와 다음 요청을 deadline 안에 완료할 수 있을 때만 재시도
- [x] 일일 예산 소진 429의 호출 내부 재시도 금지
- [x] 재시도 대기 전에 이미 취소된 상위 signal의 즉시 전파

## Recommendations

1. Report 단계에서는 90% 달성과 남은 품질 평가 공백을 함께 기록한다.
2. 원격 배포 전 최소 5개 연구 분야 평가 데이터셋과 응답시간·비용 측정을 별도 수행한다.
3. 실제 성능 측정에서 필요할 때만 동시성 3을 도입하고 요청 예산의 원자성을 유지한다.
4. 반복 주제 페널티와 DOI 버전 그룹화는 평가 결과가 필요성을 보여줄 때 후속 개선한다.

## Next Steps

- [x] `$pdca iterate paper-concept-path-mcp` 2차 반복 완료
- [x] 재분석 일치율 90.0% 달성
- [ ] `$pdca report paper-concept-path-mcp`로 완료 보고서 작성

현재 일치율은 90.0%이므로 Report 단계로 진행할 수 있다.
